import HomeMetaSettings from './homeMetaSettings.constant';

// TODO: add related links and names here
const DefaultMetaSettings = {
  name: '',
  domain: '',
  description: HomeMetaSettings.description,
} as const;

export default DefaultMetaSettings;
